## Agent skills

### Issue tracker

Issues tracked as local markdown files under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: one `CONTEXT.md` at repo root, architecture decisions in `docs/adr/`. See `docs/agents/domain.md`.
